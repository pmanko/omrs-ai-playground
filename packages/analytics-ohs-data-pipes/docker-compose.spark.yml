services:
  spark:
    image: ${SPARK_IMAGE:-docker.io/bitnami/spark:3.3}
    hostname: spark-thriftserver
    command:
      - sbin/start-thriftserver.sh
      - --driver-memory
      - 5g
    environment:
      - HIVE_SERVER2_THRIFT_PORT=10000
    volumes:
      - ohs-dwh:/dwh
    networks:
      - spark
    healthcheck:
      test: beeline help || exit 1
      interval: 10s
      retries: 10
      start_period: 5s
      timeout: 60s

volumes:
  ohs-dwh:
    driver: local

networks:
  open-health-stack:
    name: open-health-stack
    external: true

